# This project uses npm, not yarn.
#
# The frontend lives in code-review-trainer-app/. This root-level guard catches
# yarn commands run from the repository root; the same script is wired up in
# code-review-trainer-app/.yarnrc for commands run from the app directory.
yarn-path "./code-review-trainer-app/scripts/ensure-npm.cjs"
