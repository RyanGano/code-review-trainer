# This project uses npm. The frontend package lives in code-review-trainer-app/,
# which is where install and build commands should be run.
#
# Note: package-lock.json IS committed. A previous `package-lock=false` setting
# here suppressed lockfile generation entirely, which defeats reproducible
# installs and prevents Azure Static Web Apps from detecting the package manager.
save-exact=false
