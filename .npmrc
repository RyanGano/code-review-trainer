# This project uses Yarn for package management
# Please use 'yarn' instead of 'npm'
# Run 'yarn install' instead of 'npm install'

# If you must use npm, these settings will help maintain compatibility
package-lock=false
save-exact=false